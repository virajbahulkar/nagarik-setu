{
  "name": "nagarik-setu",
  "version": "0.1.0",
  "description": "NagrikSetu (Citizen Bridge) - Civic grievance and lost-property platform for Indian cities, towns and rural panchayats",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web",
    "apps/mobile",
    "packages/shared",
    "packages/database",
    "packages/config"
  ],
  "scripts": {
    "dev": "docker-compose up",
    "dev:down": "docker-compose down",
    "dev:logs": "docker-compose logs -f",
    "db:migrate": "docker-compose exec api npm run db:migrate",
    "db:seed": "docker-compose exec api npm run db:seed",
    "test": "docker-compose exec api npm run test",
    "lint": "docker-compose exec api npm run lint"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
